#include <stdio.h>

    int main(){
    	
        float alunoa, alunob, media;
            printf("Digite a nota do aluno A: ");
        scanf("%f", &alunoa);
            printf("Digite a nota do aluno B: ");
        scanf("%f", &alunob);

        media = (alunoa + alunob) / 2;

    printf("A media eh : %f",media);
        return 0;
    };
    